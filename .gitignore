# Virtual environment
.venv/

# PyInstaller
build/
dist/

# Inno Setup output
installer/

# Python cache
__pycache__/
*.pyc
*.pyo

# App state & history (user data)
app_state.json
history.json
max_poster.log
stats_cache.json
ui_prefs.json

# Secrets
.env

# Excel база (большой бинарник, не трекаем)
max_address.xlsx

# Шрифты (бинарники, восстанавливаются при разработке)
fonts/

# Twemoji (407 PNG, не трекаем)
twemoji/

# Вложенный QR-проект — имеет собственный .git, не трекаем в основном репо
app/my_qr_app/

# Claude Code служебные файлы
.claude/

# Windows
desktop.ini
Thumbs.db
